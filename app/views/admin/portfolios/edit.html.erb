<%= form_for(portfolio, url: admin_portfolio_path(portfolio), method: :put) do |f| %>
  <%= label_tag(:name, 'Name') %>
  <%= f.text_field(:name) %>
  <%= label_tag(:name, 'Description') %>
  <%= f.text_field(:description) %>
  <%= f.submit('Update') %>
<% end %>
<%= link_to 'Back', admin_path %>
